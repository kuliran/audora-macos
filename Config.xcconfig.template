// Optional local overrides for the local-first fork.
// Copy this file to Config.xcconfig only if you need to change the defaults.
// Config.xcconfig is ignored and must never contain committed credentials.

SWIFT_ACTIVE_COMPILATION_CONDITIONS = $(inherited) AUDORA_LOCAL_SETUP
INFOPLIST_PREPROCESS = YES
INFOPLIST_PREPROCESSOR_DEFINITIONS = $(inherited) AUDORA_LOCAL_SETUP=1
CLERK_PUBLISHABLE_KEY =
CONVEX_DEPLOYMENT_URL = http:/$()/127.0.0.1:3210
AUDORA_PRODUCT_BUNDLE_IDENTIFIER = com.audora.local
