# RFC media

This directory contains source diagrams and future visual references used by the
central RFC. Keep editable sources here and link them from `../README.md`; do not
store screenshots containing recordings, transcripts, credentials, user paths, or
other private data.

- [`system-context.mmd`](system-context.mmd): product processes and data boundaries.
- [`layers.mmd`](layers.mmd): dependency direction between architecture layers.
